// import {  } from '@testing-library/react';

// local imports
import { renderWithStore } from '../../utils/testHelpers';
import AboutPage from './About';

test('AboutPage renders', () => {
  renderWithStore(<AboutPage />);
});
